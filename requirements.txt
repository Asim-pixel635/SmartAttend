# SmartAttend - Smart Attendance System with Face Recognition
# Requirements file - install with: pip install -r requirements.txt

# Web framework for backend API
Flask==3.0.3

# Cross-Origin Resource Sharing for frontend communication
flask-cors==4.0.0

# Computer vision library for webcam access and image processing
opencv-python==4.9.0.80

# Face detection and recognition library
face-recognition==1.3.0

# Numerical operations for face encoding calculations
numpy==1.26.4

# Note: face-recognition requires dlib which needs CMake installed.
# On Windows, install CMake first: https://cmake.org/download/
# Then run: pip install dlib before installing face-recognition
# Or use: pip install face-recognition

