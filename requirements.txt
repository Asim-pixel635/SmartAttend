# SmartAttend - Smart Attendance System with Face Recognition
# Requirements file - install with: pip install -r requirements.txt

# Web framework for backend API
Flask>=3.0.0

# Cross-Origin Resource Sharing for frontend communication
flask-cors>=4.0.0

# Computer vision library for webcam access and image processing
opencv-python>=4.8.0

# Numerical operations for face encoding calculations
numpy>=1.26.0

# Face detection and recognition library (OPTIONAL - for better accuracy)
# NOTE: Requires dlib which needs CMake and a C++ compiler
# On Windows, install Visual Studio Build Tools first
# Then run: pip install face-recognition
# face-recognition>=1.3.0

# If face_recognition is not available, the system will use OpenCV's built-in
# face detector as a fallback (slightly less accurate but works out of the box)

