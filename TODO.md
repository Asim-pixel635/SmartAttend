# Fix Plan
1. Update app.py: validate request.json, ensure name is non-empty, return 400 on bad input, add GET /attendance route. ✅
2. Update index.html: check for empty name before sending, add fetch catch for network/server errors. ✅

